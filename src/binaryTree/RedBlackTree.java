package binaryTree;

public class RedBlackTree {

    // Binary Search Tree
    // extra balance operation during insertion, updating and deletion
    // use Red and Black color to maintain balance -> to improving performance

//    Each node should have either the color red or black.
//    The root node should always be black.
//    A red node should not have a parent and child having red color.
//    Each path from a node to any of its descendant's NULL nodes has the same number of black nodes.

    //1. root must be black
    //2. external must be black
    //3. red node must have black child
    //4. all external black node shd have same depth




}
